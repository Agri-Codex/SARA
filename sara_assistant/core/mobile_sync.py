class MobileSync:
    """Placeholder for future Android/iOS sync layer."""

    @staticmethod
    def sync_status() -> str:
        return (
            "Mobile sync framework ready. Full cloud/mobile companion app requires "
            "backend API + authentication layer in future release."
        )
